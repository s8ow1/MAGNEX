{
  "name": "magnex-backend",
  "version": "1.0.0",
  "description": "Backend for MAGNEX — serves the static site and proxies /api/chat to the Anthropic API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
